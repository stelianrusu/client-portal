{
    "routes": [
        {
            "route": "*",
            "allowedRoles": ["authenticated"]
        },
        {
        "route": "/login",
        "rewrite": "/.auth/login/aad"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html"
    }
}